{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <section class="container">
        <h1>Edit Book</h1>

        {{ include('book/_form.html.twig', {'button_label': 'Sauvegarder les modifications'}) }}

        <a href="{{ path('app_book_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>

        {# {{ include('book/_delete_form.html.twig') }} #}
    </section>
{% endblock %}
